<%= stylesheet_link_tag "main", media: "all" %>
<%if !@user%>
  <div class="main-container">
    <div class="control-container">
      <a class="button-primary" href="/sign_in">Log In</a>
      <a class="button-primary" href="/registration">Sign Up</a>
    </div>
  </div>
<%end%>
<%if @user %>
  <div class="main-container">
    <div class="logout-container">
      <span class="login-message">Logged in as: <%= @user.username%></span>
      <div class="logout-div">
        <%= button_to "Logout", logout_path, method: :delete, class: "button-secondary logout" %>
      </div>
    </div>
    <div class="control-container">
      <a class="button-primary">People Search</a>
      <a class="button-primary">Courses Search</a>
      <a class="button-primary">Projects Search</a>
      <a class="button-primary">Theses Search</a>
    </div>
  </div>
<%end%>
